<UserControl x:Class="iBackup.Client.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:iBackup.Client.App.Views">
    <Grid>
        <Border Width="380" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="White" CornerRadius="8" Padding="32"
                BorderBrush="#E5E7EB" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Sign in to iBackup" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20" />

                <TextBlock Text="Server URL" Margin="0,0,0,4" />
                <TextBox Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="6" />

                <TextBlock Text="Email" Margin="0,0,0,4" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="6" />

                <TextBlock Text="Password" Margin="0,0,0,4" />
                <PasswordBox x:Name="PasswordInput" Margin="0,0,0,12" Padding="6"
                             PasswordChanged="OnPasswordChanged" />

                <StackPanel Visibility="{Binding IsRegisterMode, Converter={x:Static views:Converters.BoolToVisibility}}">
                    <TextBlock Text="Display name" Margin="0,0,0,4" />
                    <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="6" />
                </StackPanel>

                <TextBlock Text="{Binding Error}" Foreground="#DC2626" TextWrapping="Wrap" Margin="0,0,0,8" />

                <Button Content="Sign in" Command="{Binding SignInCommand}"
                        IsEnabled="{Binding IsBusy, Converter={x:Static views:Converters.InvertBool}}"
                        Background="{StaticResource AccentBrush}" Foreground="White"
                        Padding="0,10" BorderThickness="0" FontSize="14" />

                <Button Content="Need an account? Register" Command="{Binding ToggleModeCommand}"
                        Background="Transparent" BorderThickness="0" Foreground="{StaticResource AccentBrush}"
                        Margin="0,12,0,0" Cursor="Hand" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
