<UserControl x:Class="iBackup.Client.App.Views.DevicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Devices" Style="{StaticResource PageTitle}" />
        <TextBlock DockPanel.Dock="Top" Text="{Binding Error}" Foreground="#DC2626" TextWrapping="Wrap" />

        <Grid DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding RenameText, UpdateSourceTrigger=PropertyChanged}" Padding="6"
                     ToolTip="New name for the selected device" />
            <Button Grid.Column="1" Content="Rename selected" Command="{Binding RenameCommand}" Padding="14,6" Margin="8,0,0,0" />
        </Grid>

        <DataGrid ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}"
                  AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding DeviceName}" Width="*" />
                <DataGridTextColumn Header="OS" Binding="{Binding OperatingSystem}" Width="180" />
                <DataGridTextColumn Header="Version" Binding="{Binding ClientVersion}" Width="80" />
                <DataGridTextColumn Header="Last backup" Binding="{Binding LastBackupAtUtc}" Width="140" />
                <DataGridTextColumn Header="Last online" Binding="{Binding LastOnlineAtUtc}" Width="140" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="60" />
                <DataGridCheckBoxColumn Header="This PC" Binding="{Binding IsCurrentDevice}" Width="70" />
                <DataGridTemplateColumn Header="Actions" Width="280">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Enable/Disable" Padding="8,2" Margin="0,0,6,0"
                                        Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="Force logout" Padding="8,2" Margin="0,0,6,0"
                                        Command="{Binding DataContext.ForceLogoutCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="Remove" Padding="8,2"
                                        Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
