<UserControl x:Class="iBackup.Client.App.Views.FoldersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Backup Sources" Style="{StaticResource PageTitle}" />

        <Border DockPanel.Dock="Top" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1"
                CornerRadius="6" Padding="12" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Add a folder" FontWeight="SemiBold" Margin="0,0,0,8" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding NewFolderPath, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
                    <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseCommand}" Padding="12,6" Margin="8,0,0,0" />
                    <Button Grid.Column="2" Content="Add" Command="{Binding AddCommand}" Padding="16,6" Margin="8,0,0,0"
                            Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" />
                </Grid>
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="Excluded extensions (comma separated, e.g. .tmp, .log)" FontSize="12" Foreground="#6B7280" />
                        <TextBox Text="{Binding ExcludedExtensionsText, UpdateSourceTrigger=PropertyChanged}" Padding="4" />
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Excluded folder names (e.g. node_modules, bin, obj)" FontSize="12" Foreground="#6B7280" />
                        <TextBox Text="{Binding ExcludedFoldersText, UpdateSourceTrigger=PropertyChanged}" Padding="4" />
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <TextBlock DockPanel.Dock="Top" Text="{Binding Error}" Foreground="#DC2626" TextWrapping="Wrap" />

        <DataGrid ItemsSource="{Binding Folders}" SelectedItem="{Binding SelectedFolder}"
                  AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*" />
                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled}" Width="70" />
                <DataGridTextColumn Header="Schedule" Binding="{Binding Schedule.Type}" Width="110" />
                <DataGridTextColumn Header="Compression" Binding="{Binding Compression}" Width="100" />
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="70" />
                <DataGridTemplateColumn Header="Actions" Width="260">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Back up now" Padding="8,2" Margin="0,0,6,0"
                                        Command="{Binding DataContext.BackupNowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="Enable/Disable" Padding="8,2" Margin="0,0,6,0"
                                        Command="{Binding DataContext.ToggleEnabledCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="Remove" Padding="8,2"
                                        Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
